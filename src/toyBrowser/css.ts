export const a = 0
// function getSelectors (element) {
//   const cls = element.className.trim().split(' ')
//   const set = new Set(cls)
//   set.add(element.tagName)
//   set.add(element.id)

//   return set
// }

// ele#_id.b
// 'ele#_id.b'.split(/(?=[#.])/)

// function calcWeights(selector): number {

// }
